# Mallard Code Library

Tested, executable reference implementations of biostatistical analyses in R and Python.

Each entry is one method: a short explanation, R code, Python code wherever a faithful Python
implementation exists, a synthetic dataset with a known answer, and the tolerances the code is held
to. Every entry is run on every commit. CI checks that each executed language recovers the answer
the data was built around and, where two languages ran, that they agree with each other; the list
of entries below says which ran for each. The library is maintained by [Ask Mallard](https://askmallard.com), which uses these
entries as reference implementations, and is published so anyone can read, run or reuse them.

**This is not clinical or statistical advice.** A working implementation of a method says nothing
about whether that method answers your question, whether your data meet its assumptions, or whether
the right column reached the right argument. Have a statistician review any analysis that informs
a study.

## What each entry claims

Every entry makes **two separate claims**, and neither implies the other.

**Agreement.** Every executed language produces the same estimates from identical rows, to the
tolerances recorded in the entry's `expected.json`. Given identical data, a real difference between
two correct-looking implementations is almost always a package default, and those differ more often
than people expect. `scipy.stats.ttest_ind` runs the pooled-variance test unless told otherwise,
while R's `t.test` runs Welch's; SAS `PROC LOGISTIC` models the lower ordered value by default, so a
0/1 outcome without `event='1'` inverts every odds ratio. Reading either file alone would not reveal
this. Running both does.

**Recovery.** Those estimates are close to the parameters the synthetic data was generated from.
This tolerance is looser, because an estimate carries sampling error, and it exists to catch
implementations that agree with each other on the same wrong model.

Agreement alone passes identical mistakes; recovery alone passes a default mismatch smaller than
sampling error. `harness/check.py` reports the two separately and does not state the agreement
claim at all when only one language ran.

**What no entry claims is that your analysis is correct.**

## Entries

<!-- catalogue:start (generated by harness/catalogue.py; do not edit by hand) -->

**86 entries.** 78 are executed in both R and Python and carry both claims; 8 are executed in one language and carry the recovery claim alone, with the reason in the entry's `meta.json`.

| Entry | What it estimates | Executed in |
|---|---|---|
| [`aipw-doubly-robust`](lib/aipw-doubly-robust/) | Augmented inverse-probability weighting (AIPW): a doubly robust effect | R and Python |
| [`ancova-baseline-adjusted`](lib/ancova-baseline-adjusted/) | ANCOVA: a trial effect adjusted for the baseline value | R and Python |
| [`andersen-gill-recurrent-events`](lib/andersen-gill-recurrent-events/) | Andersen-Gill model for recurrent events | R and Python |
| [`bayesian-logistic-regression`](lib/bayesian-logistic-regression/) | Bayesian logistic regression with stated priors | R |
| [`binomial-identity-risk-difference`](lib/binomial-identity-risk-difference/) | Adjusted risk difference from an identity-link binomial model | R and Python |
| [`bivariate-diagnostic-meta-analysis`](lib/bivariate-diagnostic-meta-analysis/) | Bivariate random-effects meta-analysis of diagnostic accuracy (Reitsma) | R |
| [`bland-altman-limits`](lib/bland-altman-limits/) | Bland-Altman limits of agreement for method comparison | R and Python |
| [`bootstrap-percentile-bca`](lib/bootstrap-percentile-bca/) | Bootstrap percentile and BCa confidence intervals | R and Python |
| [`calibration-slope-intercept`](lib/calibration-slope-intercept/) | Calibration of a prediction model: slope, calibration-in-the-large and O/E | R and Python |
| [`case-cohort-weighted-cox`](lib/case-cohort-weighted-cox/) | Case-cohort analysis with a weighted Cox model | R |
| [`case-crossover-conditional-logistic`](lib/case-crossover-conditional-logistic/) | Case-crossover design by conditional logistic regression | R and Python |
| [`cause-specific-cox`](lib/cause-specific-cox/) | Cause-specific Cox regression under competing risks | R and Python |
| [`chi-square-two-by-two`](lib/chi-square-two-by-two/) | Two-by-two table: chi-square, Fisher's exact test and the odds ratio | R and Python |
| [`cluster-level-summary-analysis`](lib/cluster-level-summary-analysis/) | Cluster-level summary analysis for few clusters | R and Python |
| [`cluster-robust-cr2-small-sample`](lib/cluster-robust-cr2-small-sample/) | Small-sample cluster-robust inference: CR2 with Satterthwaite df | R and Python |
| [`clustered-prevalence-cluster-sample`](lib/clustered-prevalence-cluster-sample/) | Prevalence from a cluster sample, with a design-based interval | R and Python |
| [`cochran-armitage-trend`](lib/cochran-armitage-trend/) | Cochran-Armitage test for trend in a proportion | R and Python |
| [`comparing-two-diagnostic-tests`](lib/comparing-two-diagnostic-tests/) | Comparing two diagnostic tests on the same patients | R and Python |
| [`competing-risks-cumulative-incidence`](lib/competing-risks-cumulative-incidence/) | Competing risks: the cumulative incidence function | R and Python |
| [`complex-survey-domain-prevalence`](lib/complex-survey-domain-prevalence/) | Complex survey prevalence and domain estimation | R and Python |
| [`conditional-logistic-matched-case-control`](lib/conditional-logistic-matched-case-control/) | Conditional logistic regression for a matched case-control study | R and Python |
| [`controlled-interrupted-time-series`](lib/controlled-interrupted-time-series/) | Controlled interrupted time series (intervention against control series) | R and Python |
| [`correlation-pearson-spearman`](lib/correlation-pearson-spearman/) | Pearson and Spearman correlation with intervals | R and Python |
| [`cox-proportional-hazards`](lib/cox-proportional-hazards/) | Cox proportional hazards regression | R and Python |
| [`cox-time-varying-exposure`](lib/cox-time-varying-exposure/) | Cox regression with a time-varying exposure | R and Python |
| [`crossover-trial-two-period`](lib/crossover-trial-two-period/) | Two-period crossover trial (AB/BA) | R and Python |
| [`decision-curve-analysis`](lib/decision-curve-analysis/) | Decision curve analysis: net benefit of a prediction model | R and Python |
| [`delta-adjusted-tipping-point`](lib/delta-adjusted-tipping-point/) | Delta-adjusted tipping-point analysis for missing outcomes | R and Python |
| [`descriptive-summary-table-one`](lib/descriptive-summary-table-one/) | Table 1: baseline characteristics with standardized differences | R and Python |
| [`diagnostic-accuracy-wilson`](lib/diagnostic-accuracy-wilson/) | Diagnostic accuracy with Wilson intervals | R and Python |
| [`difference-in-differences-twfe`](lib/difference-in-differences-twfe/) | Difference-in-differences with two-way fixed effects (single adoption date) | R and Python |
| [`e-value-quantitative-bias`](lib/e-value-quantitative-bias/) | E-value and a simple quantitative bias analysis for unmeasured confounding | R and Python |
| [`fine-gray-subdistribution`](lib/fine-gray-subdistribution/) | Fine-Gray regression for the cumulative incidence of one cause | R |
| [`fisher-exact-conditional-odds-ratio`](lib/fisher-exact-conditional-odds-ratio/) | Fisher's exact test with the conditional odds ratio | R and Python |
| [`g-computation-standardization`](lib/g-computation-standardization/) | G-computation (marginal standardization) for a risk difference and risk ratio | R and Python |
| [`gee-working-correlation-robust-se`](lib/gee-working-correlation-robust-se/) | Marginal logistic regression by GEE on clustered binary data | R and Python |
| [`incidence-rate-exact-poisson`](lib/incidence-rate-exact-poisson/) | An incidence rate with an exact Poisson interval | R and Python |
| [`instrumental-variables-2sls`](lib/instrumental-variables-2sls/) | Instrumental variables by two-stage least squares (Mendelian randomization) | R and Python |
| [`interrupted-time-series-segmented`](lib/interrupted-time-series-segmented/) | Interrupted time series: segmented regression with Newey-West errors | R and Python |
| [`intraclass-correlation`](lib/intraclass-correlation/) | Intraclass correlation for inter-rater reliability, ICC(A,1) | R and Python |
| [`inverse-probability-censoring-weights`](lib/inverse-probability-censoring-weights/) | Inverse probability of censoring weights for informative dropout | R and Python |
| [`iptw-propensity-weights-robust-se`](lib/iptw-propensity-weights-robust-se/) | An average treatment effect by inverse-probability-of-treatment weighting | R and Python |
| [`kaplan-meier-logrank`](lib/kaplan-meier-logrank/) | Kaplan-Meier estimation and the log-rank test | R and Python |
| [`kruskal-wallis`](lib/kruskal-wallis/) | Kruskal-Wallis test with group medians | R and Python |
| [`linear-mixed-model-repeated-measures`](lib/linear-mixed-model-repeated-measures/) | Linear mixed model for repeated measurements | R and Python |
| [`linear-regression-robust-se`](lib/linear-regression-robust-se/) | Linear regression with heteroskedasticity-consistent standard errors | R and Python |
| [`logistic-regression-adjusted-odds-ratio`](lib/logistic-regression-adjusted-odds-ratio/) | Logistic regression for an adjusted odds ratio | R and Python |
| [`mann-whitney-hodges-lehmann`](lib/mann-whitney-hodges-lehmann/) | Mann-Whitney test with the Hodges-Lehmann shift | R and Python |
| [`mcnemar-paired-binary`](lib/mcnemar-paired-binary/) | McNemar's test for a paired binary outcome | R and Python |
| [`mediation-counterfactual-binary`](lib/mediation-counterfactual-binary/) | Causal mediation with a binary outcome (natural direct and indirect effects) | R and Python |
| [`mixed-effects-logistic-clustered`](lib/mixed-effects-logistic-clustered/) | Mixed-effects logistic regression for clustered binary outcomes | R |
| [`mixed-effects-poisson-clustered`](lib/mixed-effects-poisson-clustered/) | Mixed-effects Poisson regression for clustered counts | R |
| [`modified-poisson-adjusted-risk-ratio`](lib/modified-poisson-adjusted-risk-ratio/) | Modified Poisson regression for an adjusted risk ratio | R and Python |
| [`multinomial-logistic-regression`](lib/multinomial-logistic-regression/) | Multinomial logistic regression for an unordered categorical outcome | R and Python |
| [`multiple-imputation-chained-equations`](lib/multiple-imputation-chained-equations/) | Multiple imputation by chained equations with Rubin's rules | R |
| [`multiplicity-adjustment`](lib/multiplicity-adjustment/) | Multiplicity adjustment: Holm, Hochberg and Benjamini-Hochberg | R and Python |
| [`negative-binomial-rate-ratio`](lib/negative-binomial-rate-ratio/) | Negative binomial regression for overdispersed counts | R and Python |
| [`nested-case-control-risk-set`](lib/nested-case-control-risk-set/) | Nested case-control with risk-set sampling (conditional logistic) | R and Python |
| [`network-meta-analysis`](lib/network-meta-analysis/) | Network meta-analysis (frequentist, random effects) | R and Python |
| [`noninferiority-risk-difference`](lib/noninferiority-risk-difference/) | Noninferiority analysis of a risk difference against a margin | R and Python |
| [`one-way-anova-tukey`](lib/one-way-anova-tukey/) | One-way ANOVA with Tukey pairwise intervals | R and Python |
| [`ordinal-proportional-odds`](lib/ordinal-proportional-odds/) | Proportional-odds regression for an ordered outcome | R and Python |
| [`overlap-weights-ato`](lib/overlap-weights-ato/) | Overlap weights for the effect in the overlap population (ATO) | R and Python |
| [`paired-t-test`](lib/paired-t-test/) | Paired t-test for a within-person change | R and Python |
| [`penalized-prediction-optimism`](lib/penalized-prediction-optimism/) | Penalized prediction model development with bootstrap optimism correction | R and Python |
| [`poisson-rate-regression-overdispersed`](lib/poisson-rate-regression-overdispersed/) | Rate regression on overdispersed counts with unequal follow-up | R and Python |
| [`prediction-binary-validation`](lib/prediction-binary-validation/) | Binary prediction: discrimination, calibration and Brier score | R and Python |
| [`predictive-values-likelihood-ratios`](lib/predictive-values-likelihood-ratios/) | Predictive values and likelihood ratios | R and Python |
| [`propensity-score-matching-att`](lib/propensity-score-matching-att/) | Propensity-score matching for the effect in the treated (ATT) | R and Python |
| [`quantile-regression-median`](lib/quantile-regression-median/) | Median regression for a skewed outcome | R and Python |
| [`random-effects-meta-analysis`](lib/random-effects-meta-analysis/) | Random-effects meta-analysis: REML with Hartung-Knapp | R and Python |
| [`regression-discontinuity-local-linear`](lib/regression-discontinuity-local-linear/) | Sharp regression discontinuity: local linear with robust bias-corrected inference | R and Python |
| [`restricted-cubic-spline`](lib/restricted-cubic-spline/) | Restricted cubic spline for a nonlinear exposure | R and Python |
| [`restricted-mean-survival-time`](lib/restricted-mean-survival-time/) | Restricted mean survival time (RMST) | R and Python |
| [`risk-difference-ratio-nnt`](lib/risk-difference-ratio-nnt/) | Risk difference, risk ratio and NNT from a two-arm trial | R and Python |
| [`roc-auc-delong`](lib/roc-auc-delong/) | ROC analysis: AUC with a DeLong interval and a Youden threshold | R and Python |
| [`self-controlled-case-series`](lib/self-controlled-case-series/) | Self-controlled case series (conditional Poisson) | R and Python |
| [`single-proportion-wilson-clopper-pearson`](lib/single-proportion-wilson-clopper-pearson/) | A single proportion with Wilson and Clopper-Pearson intervals | R and Python |
| [`statistical-process-control`](lib/statistical-process-control/) | Statistical process control: u-, p- and individuals charts | R and Python |
| [`stepped-wedge-mixed-model`](lib/stepped-wedge-mixed-model/) | Stepped-wedge trial: the Hussey-Hughes mixed model | R and Python |
| [`target-trial-clone-censor-weight`](lib/target-trial-clone-censor-weight/) | Target trial emulation with a grace period: clone, censor, weight | R |
| [`weibull-accelerated-failure-time`](lib/weibull-accelerated-failure-time/) | Weibull regression: time ratio and hazard ratio | R and Python |
| [`weighted-kappa-agreement`](lib/weighted-kappa-agreement/) | Cohen's and quadratic-weighted kappa for two raters | R and Python |
| [`welch-two-sample-t-test`](lib/welch-two-sample-t-test/) | Two-sample comparison of means with unequal variances (Welch) | R and Python |
| [`wilcoxon-signed-rank-hodges-lehmann`](lib/wilcoxon-signed-rank-hodges-lehmann/) | Wilcoxon signed-rank test with the Hodges-Lehmann pseudo-median change | R and Python |
| [`zero-inflated-poisson`](lib/zero-inflated-poisson/) | Zero-inflated Poisson regression for counts with structural zeros | R and Python |

<!-- catalogue:end -->

An entry runs in R only when no Python package implements the method faithfully, or when its
randomness cannot be aligned across languages (multiple imputation draws, for example). Writing
Python that silently computed something different would be worse than having none.

## Using an entry

Each directory under `lib/` stands alone:

```
lib/<method>/
  README.md      what it estimates, what it assumes, and what it deliberately does not do
  meta.json      identity, the kind of question it answers, pinned packages and defaults
  r.R            the R implementation
  python.py      the Python implementation
  fixture.py     the seeded generator for the synthetic data (standard library only)
  fixture.csv    the data itself, committed so both languages read byte-identical rows
  expected.json  the truth, the tolerances, and how each tolerance was measured
```

To run one, install the package versions its `meta.json` pins, then run the files from inside the
entry's directory:

```bash
cd lib/cox-proportional-hazards
Rscript r.R
python python.py
```

Each script prints its estimates and ends with a machine-readable `--- HARNESS ---` block. To use
one on your own data, read the entry's README first, then replace the fixture with your data and the
fixture's column names with yours. Keep the pinned options in the code: they are the part that took
the checking to get right. Deciding which of your columns is the stratum, the cluster or the
competing event is a judgement about your study, not a find and replace.

## Running the checks

CI (`.github/workflows/verify.yml`) runs every entry's `r.R` and `python.py`, then passes their
output to `harness/check.py`, which decides both claims. To reproduce it locally, install the
versions each `meta.json` pins, then run the entries and the checker:

```bash
for entry in lib/*/; do
  name=$(basename "$entry"); args=()
  if [ -f "$entry/r.R" ]; then (cd "$entry" && Rscript r.R) > "/tmp/$name.r.txt" && args+=(--output "r=/tmp/$name.r.txt"); fi
  if [ -f "$entry/python.py" ]; then (cd "$entry" && python python.py) > "/tmp/$name.py.txt" && args+=(--output "python=/tmp/$name.py.txt"); fi
  python harness/check.py --entry "$entry" "${args[@]}"
done
```

and the harness's own checks, which CI runs first:

```bash
python harness/check.test.py              # the comparator's own controls
python harness/lint.py --root lib         # pinned options present in code, README list current, public-surface check
python harness/new_examples.test.py       # calibration and negative controls
```

The calibration files re-run each fixture over many seeds. CI uses 50; set `MALLARD_SEEDS=100` to
reproduce the 100-seed runs each `expected.json` records. `MALLARD_R_LIBS` may name an R library to
put first on the path.

## How the checks are built, and why

**Tolerances are measured, not asserted.** Each `expected.json` records the tolerance for each
output and how it was measured. Most agreement tolerances are 1e-4; some are wider where two
optimisers or two variance estimators legitimately differ, and the observed difference is recorded
beside the key. The first version of this README claimed 1e-6 everywhere, and the first real run
refuted it: two conditional logistic implementations agreed to 5.6e-6 on the same likelihood, so
1e-6 failed a correct pair. A tolerance chosen by assertion is calibrated against nothing.

**Recovery tolerances are set from repeated draws.** For entries with a calibration file
(`harness/*_examples.test.py`), the fixture is regenerated over 100 seeds and the tolerance is set
above the 99th-percentile miss. CI repeats this on 50 seeds and compares the same order statistic,
so the check is neither looser nor stricter than the one the tolerance was set from. The earliest
entries predate the calibration files: their `expected.json` records how each tolerance was
measured, and CI checks the committed fixture only.

**Negative controls.** A tolerance loose enough to allow sampling error can be loose enough to pass
the wrong estimator. The calibration files also check that the naive analysis an entry exists to
replace (ignoring clustering, censoring a competing death, a complete-case analysis under
informative missingness) misses the truth by more than the tolerance.

**The data is committed, not regenerated in each language.** R's and NumPy's random streams differ,
so "seed 42" gives two different datasets, and a cross-language comparison would then be measuring
sampling variation while appearing to measure agreement. The generator is committed beside the data
so it is reproducible, and nothing in CI regenerates it.

**The truth does not come from running this code.** An expected value recorded from the library's
own output would pass against whatever the code produced. Instead each fixture is constructed so that
the parameters it uses are the parameters the model estimates, and a published worked example is
used wherever one exists.

**Package versions are pinned, dependencies included.** An early run failed because statsmodels
imported a private scipy helper that a newer scipy had removed: pinning the packages a file names
while their dependencies float is not pinning. CI installs R packages from a dated snapshot and
checks every installed version against the pins.

**Standard errors are checked independently.** In several entries one library's reported standard
error was wrong while its estimate was right. Where the two languages disagree on a standard error,
the entry computes an independent reference before deciding which to trust, and says so in its
README.

## Fixtures are synthetic. Always.

No real dataset, and no patient data of any kind, enters this repository.

## Contributing

Issues and pull requests are welcome. A new entry needs all seven files above, measured tolerances,
a negative control, and CI passing in every language it claims. Run `python harness/catalogue.py`
to update the list of entries above.

## Licence

Apache-2.0, with a NOTICE file. This code is meant to be pasted into other people's analyses, so the
licence should not be the thing that stops them; Apache-2.0 stays permissive while adding an explicit
patent grant.
